import { BaseQuery } from '../base-query';
export class DeactivateWebSpotlightQuery extends BaseQuery {
    constructor(config, queryService) {
        super(config, queryService);
        this.config = config;
        this.queryService = queryService;
    }
    toPromise() {
        return this.queryService.deactivateWebSpotlightAsync(this.getUrl(), this.queryConfig);
    }
    getAction() {
        return this.apiEndpoints.deactivateWebSpotlight();
    }
}
//# sourceMappingURL=deactivate-web-spotlight-query.class.js.map