import { BaseQuery } from '../base-query';
export class ActivateWebSpotlightQuery extends BaseQuery {
    constructor(config, queryService, data) {
        super(config, queryService);
        this.config = config;
        this.queryService = queryService;
        this.data = data;
    }
    toPromise() {
        return this.queryService.activateWebSpotlightAsync(this.getUrl(), this.queryConfig, this.data);
    }
    getAction() {
        return this.apiEndpoints.activateWebSpotlight();
    }
}
//# sourceMappingURL=activate-web-spotlight-query.class.js.map